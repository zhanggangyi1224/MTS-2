# Core dependencies - Pin numpy for compatibility
numpy>=1.21.0,<2.0  # madmom requires numpy < 2.0
pandas>=1.3.0
scipy>=1.7.0
scikit-learn>=1.0.0
Cython>=0.29.0  # Required for building madmom

# PyTorch - DON'T install from requirements.txt, install separately with proper CUDA
# torch and torchaudio will be installed via pip with --index-url flag

# Audio processing - core requirements
librosa>=0.9.0
soundfile>=0.10.0
audioread>=2.1.9
resampy>=0.3.1

# Dataset handling
datasets>=2.0.0
huggingface-hub>=0.10.0

# Text processing - Pin compatible versions
transformers==4.36.0  # Compatible with PyTorch 2.1-2.2
sentence-transformers>=2.2.0

# Music analysis
mir_eval>=0.7

# Utilities
tqdm>=4.64.0
pyyaml>=6.0
matplotlib>=3.5.0
seaborn>=0.11.0
plotly>=5.0.0

# Optional packages (install separately)
# madmom>=0.16.1  # Has build issues, install manually
# openai-whisper>=20230314  # Large download, install if needed
# pyrubberband>=0.3.0  # Requires system library
# pedalboard>=0.7.0  # May have compatibility issues